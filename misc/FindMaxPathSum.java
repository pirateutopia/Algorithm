package misc;

//class TreeNode

public class FindMaxPathSum {
//public:
    
    /*int maxPathSum(TreeNode *root) {
        // Note: The Solution object is instantiated only once and is reused by each test case.
        return getMax(root,Integer.MIN_VALUE);
    }
    public static int getMax(TreeNode tree, Sum sum){
    	if(tree== null) return 0;
    	int left = getMax(tree.left, sum);
    	int right = getMax(tree.right, sum);
           
    	int currentSum = left + right + tree.val;
    	sum.sum = Integer.valueOf(Math.max(sum.intValue(), currentSum));
    	return Math.max(Math.max(left+ tree.val, right + tree.val), tree.val);
    }*/

};